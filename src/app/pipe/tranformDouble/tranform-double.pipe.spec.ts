import { TranformDoublePipe } from './tranform-double.pipe';

describe('TranformDoublePipe', () => {
  it('create an instance', () => {
    const pipe = new TranformDoublePipe();
    expect(pipe).toBeTruthy();
  });
});
